output_dir: /local/laurits/0908_ParTauDETR_dataset
temp_dir: /home/laurits/tmp/ntupelized_tmp # ntupelized batches; kept, so stages 2-5 can be re-run without re-ntupelizing
ntupelizer_class: DecayProductNtupelizer # or PodioROOTNtuplelizer
chunk_size: 100000 # max events per output file: z_train_00000.parquet, z_train_00001.parquet, ...
row_group_size: 1024 # rows per parquet row group
datasets:
    p8_ee_Z_tautau_ecm91:
        input_dir: "/local/joosep/mlpf/cld/v1.2.5_key4hep_2025-05-29/gen/p8_ee_Z_tautau_ecm91/root/"
        file_pattern: "*.root"
        short_name: z
        is_signal: true
        train_frac: 0.9

    p8_ee_Z_qq_ecm91:
        input_dir: "/local/joosep/mlpf/cld/v1.2.5_key4hep_2025-05-29/gen/p8_ee_Z_qq_ecm91/root/"
        file_pattern: "*.root"
        short_name: qq
        is_signal: false
        train_frac: 0.9
weights:
    produce_plots: True
    add_weights: True
    n_files_per_sample: 100000
